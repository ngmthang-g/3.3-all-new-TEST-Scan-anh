AUTO Thần Long đa tính năng Pro v4.0

Release contract:
- Windows x64 EXE and ThanLongCleanRouteBridge.dll must be deployed together (protocol 0x00040000).
- AUTO PHÓ BẢN uses managed client/PID snapshots, not nearby-player scanning.
- Each dungeon team owns 1–6 distinct clients and one leader; active teams may not overlap PIDs.
- Per-team Start/Pause/Resume/Stop is independent; mutable Bridge actions remain serialized per client.
- Dungeon entry requires gather barrier -> leader NPC/dialog -> all members prove DungeonMapID/MapReady.
- Fight stage completion requires donor strict GMonster alive->dead evidence reaching requiredKills; AOI disappearance/timeout never count as success.
- Team/preset/editor/daily counters persist through the existing Unicode INI using stable RoleID where applicable.
- Post-run selling reuses the v3.3 Auto Sell engine and fails closed on missing bag/NPC/macro proof.
- Static dungeon presets come from canonical DATA; runtime RoleID/HP/death/map/dialog state remains live authority.
- Build PASS is not real-client runtime PASS. Follow the donor live runtime test plan before calling every dungeon runtime-proven.
